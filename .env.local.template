# Privat-Instanz: ALLES lokal. Kein Cloud-Call, auch nicht query-seitig.
LLM_PROVIDER=ollama
LLM_MODEL=qwen3:14b
LLM_ENDPOINT=http://localhost:11434/v1
LLM_API_KEY=ollama

# fastembed läuft in-process (ONNX, CPU) — kein Ollama, kein Endpoint nötig.
# Multilingual wegen deutscher Inhalte. Nach erstem Ingest NICHT mehr wechseln!
EMBEDDING_PROVIDER=fastembed
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-mpnet-base-v2
EMBEDDING_DIMENSIONS=768
EMBEDDING_BATCH_SIZE=8
HUGGINGFACE_TOKENIZER=sentence-transformers/paraphrase-multilingual-mpnet-base-v2

# Die lokale Wall ist bereits physisch isoliert. Cognee 1.2 aktiviert ACL sonst
# standardmäßig und würde bei jedem Prozessstart einen neuen Principal sehen.
ENABLE_BACKEND_ACCESS_CONTROL=false

# Datenpfade werden NICHT hier gesetzt: kb/cognee_io.py leitet sie aus
# config.Instance.var_dir ab (var/<instanz>/cognee_*) — portabel für die VPS.
