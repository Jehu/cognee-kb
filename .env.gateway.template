# Gateway-Auth: Bearer-Token für PWA und iOS-Kurzbefehl (Header
# "Authorization: Bearer <Token>"). Langen Zufallswert eintragen, z. B.
# `openssl rand -hex 32`. Kopie als .env.gateway anlegen — die ist gitignored.
KB_API_TOKEN=CHANGE_ME
